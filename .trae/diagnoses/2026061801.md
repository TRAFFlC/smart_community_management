我会按“全项目体检”来审查，而不是只看未提交改动。先快速梳理项目结构、核心文档和主要源码入口，再从代码、用户体验、需求演进、视觉四个视角收敛出高优先级问题。



\### 🔍 代码审查报告



```mermaid

flowchart LR

&#x20;   A\[种子数据初始化] --> B\[sys\_role 自增主键]

&#x20;   B --> C\[代码中硬编码 role\_id]

&#x20;   C --> D\[用户绑定错角色]

&#x20;   C --> E\[菜单绑定错角色]

&#x20;   D --> F\[权限语义漂移]

&#x20;   E --> F

&#x20;   style A fill:#bbdefb,color:#0d47a1

&#x20;   style B fill:#fff3e0,color:#e65100

&#x20;   style C fill:#ffcdd2,color:#b71c1c

&#x20;   style D fill:#f3e5f5,color:#7b1fa2

&#x20;   style E fill:#f3e5f5,color:#7b1fa2

&#x20;   style F fill:#c8e6c9,color:#1a5e20

```



```mermaid

flowchart LR

&#x20;   U\[用户进入列表页] --> Q\[直接查询全表]

&#x20;   Q --> S{仅按状态生成操作}

&#x20;   S --> X\[任何有页面的人都能点流程节点]

&#x20;   Q --> Y\[数据范围未注入查询]

&#x20;   X --> Z\[职责边界失效]

&#x20;   Y --> Z

&#x20;   style U fill:#bbdefb,color:#0d47a1

&#x20;   style Q fill:#fff3e0,color:#e65100

&#x20;   style S fill:#fff3e0,color:#e65100

&#x20;   style X fill:#ffcdd2,color:#b71c1c

&#x20;   style Y fill:#ffcdd2,color:#b71c1c

&#x20;   style Z fill:#c8e6c9,color:#1a5e20

```



\#### 严重问题（必须修复）



| No. | Issue Title | Suggestion | Code Link |

|-----|-------------|------------|-----------|

| 1 | 角色体系靠硬编码自增 ID 驱动，权限语义已经漂移 | `sys\_role.id` 是自增列，却在种子数据里把 `19/18/17...` 当稳定角色 ID 使用；应改为先按 `role\_key` 建立映射，再做用户绑定和菜单授权 | \[DemoDataService.h:L172-L204](file:///d:/smart\_community\_management/src/services/DemoDataService.h#L172-L204) |

| 2 | 数据权限写在文档里，没落在查询里 | 现有 `buildDataScopeFilter()` 只处理“本人数据”，而且全文件根本没被调用；工单、事件列表直接查全表，和“社区级/街道级/小区级”权限承诺不符 | \[MainWindow.cpp:L273-L282](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L273-L282) |

| 3 | 工单/事件操作只看状态，不看当前角色职责 | “受理/派单/开始/完成/评价”“审核/分派/处理/完成”都按状态直接暴露，缺少 `role\_key`、责任人、当前节点校验；这会把闭环流程变成“谁点谁流转” | \[MainWindow.cpp:L3061-L3100](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L3061-L3100) |

| 4 | `MainWindow.cpp` 已经长成 9620 行的 God Class | 页面构造、SQL、流程、弹窗、样式全塞进一个文件，扩展新模块时只能继续堆；这不是“快”，这是把未来维护成本按年利率复利 | \[MainWindow.h:L31-L49](file:///d:/smart\_community\_management/src/widgets/MainWindow.h#L31-L49) |

| 5 | 测试只验证“能跑”，不验证“跑得对” | 登录测试只断言能登录，不断言登录后角色/菜单是否正确；页面映射测试只 `exec()` 不 `next()`，缺菜单也照样绿灯 | \[test\_core\_logic.cpp:L67-L80](file:///d:/smart\_community\_management/tests/test\_core\_logic.cpp#L67-L80) |



\#### 严重问题展开



\- 角色 ID 硬编码是当前最危险的问题：`sys\_role` 明确是自增主键 \[schema.sql:L31-L42](file:///d:/smart\_community\_management/sql/schema.sql#L31-L42)，但种子里把“超级管理员=19、业委会成员=18、街道领导=17”写死；按当前插入顺序推算，真实 ID 实际会继续后移，导致 `admin`、`jiedao\_leader`、`yewei\_zhao`、`fuwu\_sun` 都可能绑到错误角色 \[DemoDataService.h:L58-L99](file:///d:/smart\_community\_management/src/services/DemoDataService.h#L58-L99) \[DemoDataService.h:L232-L275](file:///d:/smart\_community\_management/src/services/DemoDataService.h#L232-L275)。

\- 数据权限实现几乎是空壳：辅助函数只在“个人级”时拼 `reporter\_id = 当前用户`，其余楼栋级、小区级、社区级、街道级全部直接返回空条件；更糟的是它根本没被列表查询调用，工单和事件页都直接 `WHERE del\_flag = 0` 查全表 \[MainWindow.cpp:L3012-L3043](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L3012-L3043) \[MainWindow.cpp:L5043-L5074](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L5043-L5074)。

\- 流程权限没有落在业务规则上：工单操作列纯按 `status` 生成“受理/派单/开始/完成/评价”，事件操作列也一样；这意味着居民、客服、维修、社区工作人员只要能看到该页，就可能执行不属于自己的动作 \[MainWindow.cpp:L3061-L3083](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L3061-L3083) \[MainWindow.cpp:L5088-L5129](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L5088-L5129)。



\#### 改进建议（强烈建议）



\- `MainWindow.cpp` 不该再加功能了，应该拆成 `PageWidget + QueryService + WorkflowService + PermissionPolicy` 四层；继续往里堆，只会让每次改一个页面都像在雷区里拿螺丝刀。

\- 视觉样式虽然有全局 `QSS`，但主窗口里仍然塞了大量局部 `setStyleSheet()`；设计令牌和页面样式两套体系并存，迟早互相打架 \[style.qss](file:///d:/smart\_community\_management/resources/style.qss) \[MainWindow.cpp:L67-L94](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L67-L94)。

\- 顶栏“个人中心”入口是明确死胡同，点下去只弹一句“功能开发中”，这对日常办公系统非常伤信任 \[MainWindow.cpp:L740-L750](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L740-L750)。



\#### 重构方案



\- 角色绑定不要赌自增 ID，应该按 `role\_key` 解析后再分配：



```cpp

QHash<QString, qint64> roleIdByKey;



QSqlQuery q("SELECT id, role\_key FROM sys\_role WHERE del\_flag = 0");

while (q.next()) {

&#x20;   roleIdByKey.insert(q.value("role\_key").toString(), q.value("id").toLongLong());

}



auto bindUserRole = \[\&](qint64 userId, const QString\& roleKey) {

&#x20;   db.insert("sys\_user\_role", {

&#x20;       {"user\_id", userId},

&#x20;       {"role\_id", roleIdByKey.value(roleKey)}

&#x20;   });

};



auto bindRoleMenus = \[\&](const QString\& roleKey, const QList<int>\& menuIds) {

&#x20;   qint64 roleId = roleIdByKey.value(roleKey);

&#x20;   for (int menuId : menuIds) {

&#x20;       db.insert("sys\_role\_menu", {{"role\_id", roleId}, {"menu\_id", menuId}});

&#x20;   }

};

```



\- 流程节点不要直接绑状态，先过权限策略，再决定是否显示、是否执行：



```cpp

enum class WorkOrderAction { Accept, Assign, Start, Finish, Evaluate };



static bool canOperateWorkOrder(const SysUser\& user, int status, qint64 reporterId, qint64 assigneeId) {

&#x20;   const auto roles = QSet<QString>(user.roleNames.begin(), user.roleNames.end());



&#x20;   if (status == 0) return roles.contains(QStringLiteral("物业客服"));

&#x20;   if (status == 1) return roles.contains(QStringLiteral("物业客服")) || roles.contains(QStringLiteral("物业经理"));

&#x20;   if (status == 2) return assigneeId == user.id;

&#x20;   if (status == 3) return assigneeId == user.id;

&#x20;   if (status == 4) return reporterId == user.id;

&#x20;   return false;

}

```



\#### 亮点（如有）



\- 登录页的品牌区自绘比主系统本体更有完成度，是少数让我觉得“不是模板味”的地方 \[LoginWidget.cpp:L15-L89](file:///d:/smart\_community\_management/src/widgets/LoginWidget.cpp#L15-L89)。

\- 全局样式令牌化做得不差，至少说明团队知道“统一设计语言”这件事不是玄学 \[style.qss](file:///d:/smart\_community\_management/resources/style.qss)。



\### 👤 用户模拟报告



\#### 用户画像



| 角色 | 背景 | 核心诉求 |

|------|------|----------|

| 居民新手 | 50+ 普通住户，只会完成必要操作 | 报修、看公告、缴费，不想理解系统结构 |

| 物业客服 | 高频办公用户，每天要处理几十条工单 | 快速受理、派单、别点错、别漏单 |

| 网格员 | 外勤人员，任务碎片化 | 快速上报、快速跟进、别看到无关数据 |

| 便民服务商 | 第三方协同角色，不关心社区治理全貌 | 只看自己的服务订单，能接单能完单 |



\#### 全流程体验记录



\- 步骤 1：居民新手登录后进入“报事报修” → 列表直接查全表、操作列按状态给动作 → 这不是自助服务，是把业务后台按钮塞给了普通用户，容易让人一边迷惑一边误操作 \[MainWindow.cpp:L3012-L3043](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L3012-L3043) \[MainWindow.cpp:L3061-L3100](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L3061-L3100)。

\- 步骤 2：物业客服希望只看自己小区、待受理、可派单的工单 → 代码没有真正的小区级数据隔离，也没有稳定的角色策略 → 客服体验表面上“功能挺全”，实际上边界全糊。

\- 步骤 3：网格员进入事件中心 → 看到的是全量事件列表，操作按钮仍按状态驱动 → 外勤角色最需要“少而准”，现在得到的是“多而乱” \[MainWindow.cpp:L5043-L5074](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L5043-L5074)。

\- 步骤 4：服务商 `fuwu\_sun` 本应只处理便民服务 → 种子账号角色绑定疑似错位，极可能进错菜单、看错数据 → 协同角色会第一时间怀疑“这系统不是给我用的” \[DemoDataService.h:L232-L275](file:///d:/smart\_community\_management/src/services/DemoDataService.h#L232-L275)。



\#### 功能缺失清单



\- 个人中心被规格列为基础能力，但当前入口是空壳提示，不是功能 \[functional\_spec.md:L21-L26](file:///d:/smart\_community\_management/.trae/specs/functional\_spec.md#L21-L26) \[MainWindow.cpp:L740-L750](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L740-L750)。

\- 多角色视角切换在规格里是明确需求，但当前登录态只是一份 `currentUser`，没有真正的角色视图切换模型 \[functional\_spec.md:L21-L26](file:///d:/smart\_community\_management/.trae/specs/functional\_spec.md#L21-L26)。

\- 服务商“只看自己的工单、接单、完单”是典型协同路径，但系统最关键的权限与角色绑定先天不稳，后续体验自然站不住 \[use\_cases.md:L30-L36](file:///d:/smart\_community\_management/.trae/specs/use\_cases.md#L30-L36)。



\#### 体验痛点



\- `高`：用户不知道哪些操作是“我该做的”，因为系统把“状态按钮”伪装成“权限按钮”。

\- `高`：数据隔离承诺与实际不符，任何角色一旦看到超出职责的数据，信任感就直接归零。

\- `中`：顶栏有“个人中心”却只有提示，像把“请走这边”写在墙上，门后面是一堵砖。

\- `中`：操作列是彩色文字，不像按钮，点击发现能操作，不点击又以为只是状态说明 \[MainWindow.cpp:L260-L270](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L260-L270)。



\#### 激励型需求建议



\- “如果系统能只给我看我该看的数据、只给我能做的动作，我会觉得它在帮我，不是在考我。”

\- “如果客服/网格员/服务商进入页面就是自己的待办上下文，而不是一张全量表，我会愿意每天打开它。”

\- “如果个人中心能真正承载资料、密码、角色切换和常用入口，这个系统才像长期使用的办公产品，而不是课程演示壳子。”



\### 📋 需求审查报告



\#### 核心需求回顾



\- 系统定位：连接居民、物业、业委会、社区、街道与协同机构的一体化治理与服务平台 \[spec.md:L23-L33](file:///d:/smart\_community\_management/.trae/specs/spec.md#L23-L33)。

\- 核心用户：多角色、多层级、多组织主体，重点不是“页面多”，而是“职责清、数据隔离、流程闭环” \[use\_cases.md:L13-L36](file:///d:/smart\_community\_management/.trae/specs/use\_cases.md#L13-L36)。

\- 核心痛点：报修反馈慢、事件协同碎、数据靠人工汇总、不同组织边界不清 \[spec.md:L137-L166](file:///d:/smart\_community\_management/.trae/specs/spec.md#L137-L166)。



\#### 需求覆盖度评估



| 需求项 | 重要度 | 当前状态 | 评价 |

|--------|--------|----------|------|

| 统一认证与基础门户 | P0 | 部分 | 登录和首页有，但个人中心、角色视图切换残缺 |

| 多角色权限与数据隔离 | P0 | 部分偏失效 | 文档写得像政务平台，代码落地像“全员共享后台” |

| 报修/事件闭环 | P0 | 部分 | 状态节点有，职责约束和范围控制没落下去 |

| 多组织协同服务 | P1 | 薄弱 | 服务商路径最先被角色错配拖垮 |

| 统计分析 | P1 | 已实现基础 | 能展示，但可信度依赖前置流程数据是否干净 |

| 智能问答 | P2 | 已实现基础 | 适合演示，但不是当前平台真正的短板缓解器 |



\#### 偏离预警 🚨



\- \[偏离描述] 把“模块数量”当成完成度 → \[影响] 页面很多，真正稳定的职责链很少 → \[纠偏建议] 先把 P0 的权限、数据隔离、闭环流程做实，再谈扩面。

\- \[偏离描述] 规格按多组织治理平台写，代码按单文件桌面演示写 → \[影响] 一加需求就全局串改，无法承接角色细化和组织扩张 → \[纠偏建议] 先拆业务边界，再继续加页面。

\- \[偏离描述] 过早追求“看起来全都有” → \[影响] 用户点进页面后经常遇到读多写少、入口空壳、边界失真 → \[纠偏建议] 砍掉低频演示页，把高频流程做深。



\#### 模拟需求变更



\- 新需求 1：同一用户可切换“居民/业委会成员/志愿者”多视角 → 合理性高 → 现有架构承接能力弱；当前 `currentUser` 模型和角色绑定方式不足以支撑安全切换。

\- 新需求 2：街道领导只能看本街道汇总，社区工作人员只能看本社区明细 → 合理性极高 → 现有架构承接能力差；因为查询是散落在 `MainWindow.cpp` 的手写 SQL，不是统一数据访问层。

\- 新需求 3：服务商只能看到分配给自己的订单并回填凭证 → 合理性高 → 现有架构先要修角色错配，再补订单策略，否则一上线就串数据。



\#### 战略建议



\- 项目应聚焦于：`登录/角色/权限`、`报修闭环`、`事件闭环`、`消息与待办` 这四根主梁。

\- 建议暂缓：就业服务、过多统计页、花式扩展页；这些不是平台站住脚的原因。

\- 建议砍掉：任何只有列表展示、没有真实职责动作的“看板式空壳页面”，它们只会稀释团队注意力。



\### 🎨 视觉审查报告



\#### 整体印象



\- 一句话总评：登录页像认真设计过的产品首页，主系统像把很多后台模板拼在一起的中间稿。

\- 视觉评分：6.5 / 10



\#### 严重视觉问题（让我想关掉页面）



\- `操作列` 的交互表现太弱：`createActionItem()` 本质上只是彩色文字表格项，不像按钮，用户很难形成“这里可操作”的直觉 \[MainWindow.cpp:L260-L270](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L260-L270)。

\- 登录页与主系统气质断裂：登录页有品牌绘制、深浅对比和明确记忆点，进入主系统后瞬间跌回“白卡片+表格海”，前后像两个团队做的 \[LoginWidget.cpp:L15-L89](file:///d:/smart\_community\_management/src/widgets/LoginWidget.cpp#L15-L89)。

\- 视觉一致性被大量局部 `setStyleSheet()` 侵蚀：你明明已经有全局 `style.qss`，却还在主窗口里四处硬塞局部样式，这种做法短期快，长期必然越来越脏 \[style.qss](file:///d:/smart\_community\_management/resources/style.qss) \[MainWindow.cpp:L67-L94](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L67-L94)。

\- 顶栏“个人中心”是视觉欺骗：它长得像正式入口，点击却只是提示开发中，这会直接拉低整套产品的专业感 \[MainWindow.cpp:L740-L750](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L740-L750)。



\#### 视觉改进清单



\- `表格操作区`：把彩色文本改成真正的轻量按钮或 pill button，并增加 hover/pressed 明确信号。

\- `主系统首页`：延续登录页的品牌语言，至少把顶部欢迎区和核心工作台做出更稳定的视觉记忆点，而不是清一色业务表格。

\- `样式体系`：把颜色、圆角、边框、间距收回到统一 token，减少页面级散装 QSS。

\- `空状态与占位页`：不要再用“功能开发中”充数；空状态应该告诉用户下一步能做什么，而不是提醒用户项目还没做完。



\#### 视觉亮点



\- 登录页品牌区的自绘处理有辨识度，不是廉价渐变大字报 \[LoginWidget.cpp:L15-L89](file:///d:/smart\_community\_management/src/widgets/LoginWidget.cpp#L15-L89)。

\- `style.qss` 的色彩层级、字体和间距基线相对清晰，说明项目有过设计语言意识。

\- 页面头部组件 `createPageHeader()` 提供了基本统一感，这是值得保留的骨架 \[MainWindow.cpp:L120-L157](file:///d:/smart\_community\_management/src/widgets/MainWindow.cpp#L120-L157)。



\*\*综合诊断总结\*\*



\- 先修权限底座：去掉所有角色 ID 硬编码，统一按 `role\_key` 绑定用户和菜单。

\- 先修数据隔离：建立统一查询层，把楼栋/小区/社区/街道范围真正注入 SQL。

\- 先修流程职责：工单和事件的每个动作都必须经过角色、责任人、状态三重校验。

\- 先拆主窗口：把 `MainWindow.cpp` 从 9620 行肢解成页面、服务、策略三类模块。

\- 先砍空壳功能：个人中心、低频页面、只读列表页要么补完，要么别挂成正式入口。



\- 审查方式：本次基于源码、QSS、规格文档的静态审查完成，未实际跑 GUI 交互；但仅凭当前代码，以上问题已经足够确定，不属于“可能有风险”，而是“现在就已经错位”。



